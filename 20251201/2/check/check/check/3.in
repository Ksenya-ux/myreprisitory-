; Проверка деления на ноль + ветка ifeq

    store 1 a      ; a = 1
    store 0 z      ; z = 0

    ifeq a z skip  ; 1 == 0? нет, переход не сработает

    div a z r      ; r = a / z -> ошибка -> r = inf
    out r

skip:   out a
