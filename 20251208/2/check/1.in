import asyncio, random

async def main(A):
    tasks, B = await mtasks(A)
    print(len(tasks))
    random.shuffle(tasks)
    await asyncio.gather(*tasks)
    return B

random.seed(55)
A = random.choices(range(20), k=8)
B = asyncio.run(main(A))
print(*A)
print(*B)

